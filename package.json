{
  "name": "toolsphere",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace frontend",
    "build:frontend": "npm run build --workspace frontend",
    "build:backend": "npm run test --workspace backend"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.20"
  }
}
